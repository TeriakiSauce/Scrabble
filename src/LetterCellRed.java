public class LetterCellRed extends LetterCell{
    /**
     * Create a new letter cell.
     *
     * @param x      The x position.
     * @param y      The y position.
     * @param letter The letter.
     */
    public LetterCellRed(Integer x, Integer y, Character letter) {
        super(x, y, letter);
    }
}
