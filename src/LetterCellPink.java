public class LetterCellPink extends LetterCell{
    /**
     * Create a new letter cell.
     *
     * @param x      The x position.
     * @param y      The y position.
     * @param letter The letter.
     */
    public LetterCellPink(Integer x, Integer y, Character letter) {
        super(x, y, letter);
    }
}
